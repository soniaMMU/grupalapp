<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="61dp"
        android:layout_marginTop="24dp"
        android:text="Crea tu Tienda!"
        android:textColor="#3F51B5"
        android:textSize="35sp" />

    <EditText
        android:id="@+id/etNombreTienda"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de la tienda"
        android:layout_marginBottom="8dp" /> <!-- Puedes agregar un margen si lo deseas -->

    <EditText
        android:id="@+id/etDescripcionTienda"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Descripción de la tienda"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etFotoTienda"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enlace de la imagen de la tienda"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/btnAgregarTienda"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Agregar tienda" />

</LinearLayout>
